server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_carbon?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "668157"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

carbon:
  ai:
    confidence-threshold: 0.80
    baidu:
      # 通用物体和场景识别 API (兼容性最好，在百度云平台默认开通)
      endpoint: https://aip.baidubce.com/rest/2.0/image-classify/v2/advanced_general
      api-key: "JHvZ30ANA9ORAddAiC71pA83"
      secret-key: "43ZQ8EmvMsQD2IbVOVhaefitMrXFDS7L"
  audit:
    # Non-tamper audit log placeholder for compliance narratives.
    enabled: true
  blockchain:
    # Set to false in development to disable all on-chain operations.
    enabled: false
    # true: Asynchronous mode (non-blocking). false: Synchronous mode (waits for transaction confirmation).
    async-enabled: true

    contracts:
      carbon-points: "0xPASTE_CARBON_POINTS_ADDRESS_HERE"
      audit-trail: "0xPASTE_AUDIT_TRAIL_ADDRESS_HERE"

app:
  request-timeout-ms: 8000
  auth:
    token: ""
