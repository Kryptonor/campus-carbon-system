# Backend

Spring Boot backend for AI verification and business APIs.

## Requirements
- Java 21
- Maven 3.9+
- MySQL 8.0+

## Run
1. Update `src/main/resources/application.yml` with DB credentials.
2. Execute `docs/DATABASE.sql` to initialize the schema.
3. Start the app: `mvn spring-boot:run`
